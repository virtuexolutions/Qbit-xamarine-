<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="QBid.Views.ContentViews.CustomLoaderWait">
  <ContentView.Content>
        <Grid>
            <Frame Padding="0" CornerRadius="10" HeightRequest="100" Opacity="0.6" WidthRequest="150" HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand" HasShadow="False" BackgroundColor="{StaticResource AppGrayColor}">
                <StackLayout Orientation="Vertical" Opacity="0.8">
                    <ActivityIndicator x:Name="activityIndicator" Margin="0,20,0,0"
							   Color="{StaticResource AppBlackColor}"
							   WidthRequest="40"
							   HeightRequest="40"                                      
							   HorizontalOptions="Center"
							   VerticalOptions="Center"/>
                    <Label Text="Please wait..." TextColor="{StaticResource AppBlackColor}" HorizontalTextAlignment="Center" FontAttributes="Bold" VerticalOptions="End"  Margin="0,5,0,0"></Label>
                </StackLayout>
            </Frame>
        </Grid>
    </ContentView.Content>
</ContentView>